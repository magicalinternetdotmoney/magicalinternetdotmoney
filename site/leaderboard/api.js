{"command":"python3 << 'PYEOF'\nimport json, re, os\nfrom pathlib import Path\n\nSESSION_ROOT = Path(\"/Users/stacc/.grok/sessions/%2FUsers%2Fstacc%2Fanchor-transfer-hook\")\nOUT = Path(\"/Users/stacc/anchor-transfer-hook/site/leaderboard\")\nOUT.mkdir(parents=True, exist_ok=True)\n\nTARGETS = {\n    'db.js': ['CREATE TABLE IF NOT EXISTS lev_markets', 'function ensureSchema', 'module.exports'],\n    'chain.js': ['readPoolReserves', 'getSignaturesForAddress', 'module.exports'],\n    'indexer.js': ['async function runIndexer', 'reindexMarket', 'module.exports'],\n    'api.js': ['function listMarkets', 'module.exports = { listMarkets', '// Leaderboard read API'],\n    'server.js': ['lbApi = require', '/api/leaderboard', 'LB_INDEX'],\n}\n\ndef clean_numbered(text):\n    \"\"\"Extract file body from Read tool output with N→ or N| prefixes.\"\"\"\n    if not text:\n        return None\n    lines = text.split('\\n')\n    out = []\n    numbered = 0\n    for ln in lines:\n        m = re.match(r'^(\\d+)→(.*)$', ln)\n        if m:\n            out.append(m.group(2))\n            numbered += 1\n            continue\n        m2 = re.match(r'^\\s*(\\d+)\\|(.*)$', ln)\n        if m2:\n            out.append(m2.group(2))\n            numbered += 1\n            continue\n        # duplicate: \"    10|    10|content\"\n        m3 = re.match(r'^\\s*(\\d+)\\|\\s*(\\d+)\\|(.*)$', ln)\n        if m3:\n            out.append(m3.group(3))\n            numbered += 1\n            continue\n    if numbered >= 3:\n        return '\\n'.join(out)\n    return None\n\ndef score_blob(text, fname):\n    if not text or len(text) < 100:\n        return 0\n    score = len(text)\n    sigs = TARGETS.get(fname, [])\n    for s in sigs:\n        if s in text:\n            score += 10000\n    if 'module.exports' in text:\n        score += 5000\n    # penalize grep/workspace results\n    if '<workspace_result' in text[:500]:\n        score -= 50000\n    if 'chat_history.jsonl' in text[:1000]:\n        score -= 30000\n    return score\n\ndef extract_from_write_args(args_str):\n    try:\n        args = json.loads(args_str) if isinstance(args_str, str) else args_str\n        path = args.get('path', '')\n        contents = args.get('contents', '')\n        return path, contents\n    except:\n        return None, None\n\nfound = {k: [] for k in TARGETS}\n\ndef process_text_blob(text, source, hint=None):\n    for fname in TARGETS:\n        if hint and hint != fname:\n            continue\n        # Direct file content (starts with comment or const)\n        if fname == 'api.js' and '// Leaderboard read API' in text:\n            cleaned = clean_numbered(text)\n            body = cleaned if cleaned else text\n            if len(body) > 200:\n                found[fname].append((score_blob(body, fname), body, source))\n        if fname == 'db.js' and 'CREATE TABLE IF NOT EXISTS lev_markets' in text:\n            cleaned = clean_numbered(text)\n            body = cleaned if cleaned else text\n            if len(body) > 200:\n                found[fname].append((score_blob(body, fname), body, source))\n        if fname == 'chain.js' and 'readPoolReserves' in text:\n            cleaned = clean_numbered(text)\n            body = cleaned if cleaned else text\n            if len(body) > 200:\n                found[fname].append((score_blob(body, fname), body, source))\n        if fname == 'indexer.js' and 'runIndexer' in text:\n            cleaned = clean_numbered(text)\n            body = cleaned if cleaned else text\n            if len(body) > 200:\n                found[fname].append((score_blob(body, fname), body, source))\n        if fname == 'server.js' and 'lbApi' in text and 'leaderboard' in text:\n            cleaned = clean_numbered(text)\n            body = cleaned if cleaned else text\n            if len(body) > 1000:\n                found[fname].append((score_blob(body, fname), body, source))\n\ndef walk(obj, source, depth=0):\n    if depth > 30:\n        return\n    if isinstance(obj, dict):\n        # Write tool\n        fn = obj.get('function') or {}\n        if isinstance(fn, dict) and fn.get('name') in ('Write', 'write'):\n            path, contents = extract_from_write_args(fn.get('arguments', ''))\n            if path and contents:\n                for fname in TARGETS:\n                    if f'leaderboard/{fname}' in path.replace('\\\\','/') or (fname == 'server.js' and path.endswith('server.js')):\n                        found[fname].append((score_blob(contents, fname), contents, source + ':write'))\n        # tool_result\n        if obj.get('type') == 'tool_result':\n            content = obj.get('content', '')\n            if isinstance(content, str):\n                process_text_blob(content, source + ':tool_result')\n        for k, v in obj.items():\n            if isinstance(v, str) and len(v) > 300:\n                process_text_blob(v, source + f':{k}')\n            walk(v, source, depth+1)\n    elif isinstance(obj, list):\n        for item in obj:\n            walk(item, source, depth+1)\n    elif isinstance(obj, str) and len(obj) > 300:\n        process_text_blob(obj, source)\n\n# Scan all session files\nfor fp in SESSION_ROOT.rglob('*'):\n    if not fp.is_file():\n        continue\n    if fp.suffix not in ('.jsonl', '.json', '.log', '.txt'):\n        continue\n    try:\n        if fp.suffix == '.jsonl':\n            with open(fp) as fh:\n                for i, line in enumerate(fh, 1):\n                    try:\n                        obj = json.loads(line)\n                        walk(obj, f\"{fp}:{i}\")\n                    except:\n                        pass\n        elif fp.suffix == '.json':\n            with open(fp) as fh:\n                try:\n                    walk(json.load(fh), str(fp))\n                except:\n                    pass\n        else:\n            text = fp.read_text(errors='replace')\n            if any(s in text for s in ['lev_markets', 'readPoolReserves', 'runIndexer', 'Leaderboard read API', 'lbApi']):\n                walk(text, str(fp))\n    except Exception as e:\n        pass\n\n# Also extract from raw file search for signature blocks\nSIG_FILES = {\n    'db.js': 'CREATE TABLE IF NOT EXISTS lev_markets',\n    'chain.js': 'async function readPoolReserves',\n    'indexer.js': 'async function runIndexer',\n    'api.js': 'module.exports = { listMarkets',\n}\n\nfor fname, sig in SIG_FILES.items():\n    for fp in SESSION_ROOT.rglob('updates.jsonl'):\n        text = fp.read_text(errors='replace')\n        idx = 0\n        while True:\n            pos = text.find(sig, idx)\n            if pos == -1:\n                break\n            # extract surrounding context - look for start of file content\n            start = max(0, pos - 500)\n            end = min(len(text), pos + 15000)\n            chunk = text[start:end]\n            # try to unescape JSON string\n            chunk_unescaped = chunk.replace('\\\\n', '\\n').replace('\\\\\"', '\"').replace('\\\\\\\\', '\\\\')\n            cleaned = clean_numbered(chunk_unescaped)\n            if cleaned and score_blob(cleaned, fname) > 5000:\n                found[fname].append((score_blob(cleaned, fname), cleaned, f\"{fp}:raw@{pos}\"))\n            idx = pos + len(sig)\n\nprint(\"=== RECOVERY RESULTS ===\")\nbest = {}\nfor fname, candidates in found.items():\n    if not candidates:\n        print(f\"{fname}: NOT FOUND\")\n        continue\n    candidates.sort(key=lambda x: -x[0])\n    best[fname] = candidates[0]\n    sc, body, src = candidates[0]\n    nlines = body.count('\\n') + 1\n    has_exports = 'module.exports' in body\n    print(f\"{fname}: score={sc}, lines={nlines}, chars={len(body)}, exports={has_exports}\")\n    print(f\"  source: {src[:120]}\")\n    print(f\"  candidates: {len(candidates)}\")\n    if len(candidates) > 1:\n        for c in candidates[1:3]:\n            print(f\"    alt: score={c[0]}, lines={c[1].count(chr(10))+1}, {c[2][:80]}\")\n\n# Write best candidates\nwritten = {}\nfor fname in ['db.js', 'chain.js', 'indexer.js', 'api.js']:\n    if fname not in best:\n        continue\n    sc, body, src = best[fname]\n    if sc < 3000:\n        print(f\"SKIP {fname}: score too low\")\n        continue\n    out_path = OUT / fname\n    out_path.write_text(body + ('\\n' if not body.endswith('\\n') else ''))\n    written[fname] = out_path\n    print(f\"WROTE {out_path}\")\n\n# server.js - save snippet separately\nif 'server.js' in best:\n    sc, body, src = best['server.js']\n    Path('/tmp/recovered_server_snippet.js').write_text(body)\n    print(f\"server.js snippet saved: {body.count(chr(10))+1} lines\")\n\nprint(f\"\\nWritten: {list(written.keys())}\")\nPYEOF","description":"Deep extraction from all session JSONL files","block_until_ms":180000.0}

